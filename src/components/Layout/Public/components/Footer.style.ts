import { Layout } from "antd";
import styled from "styled-components";

const { Content } = Layout;

export const FooterStyled = styled(Content)`
  display: flex;
  margin: 0 auto;
  width: 1912px;
  height: 160px;
  padding: 0 15px;
  background-color: #1d1c1c;
`;
